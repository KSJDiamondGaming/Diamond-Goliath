'use strict';

// Canonical API entry for the Tickets flagship module.
// The existing production router remains the implementation source during
// staged consolidation so no ticket workflow is lost.

module.exports = require('../../server/routes/tickets');
