name: Apply Moderation Appeal Queue Layout

on:
  push:
    branches: [dev]
    paths:
      - 'scripts/mod-appeal-layout-patch-temp.js'
      - '.github/workflows/mod-appeal-layout-once.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: dev
          fetch-depth: 0

      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply Appeal Queue layout change
        run: node scripts/mod-appeal-layout-patch-temp.js

      - name: Remove temporary patch files
        run: |
          rm -f scripts/mod-appeal-layout-patch-temp.js
          rm -f .github/workflows/mod-appeal-layout-once.yml

      - name: Validate source
        run: |
          node --check src/core/administration/mod/cases.js
          npm run doctor

      - name: Commit clean source to dev
        run: |
          git config user.name "KSJHub Actions"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "refactor(mod): rebalance appeal queue controls"
          git push origin dev
