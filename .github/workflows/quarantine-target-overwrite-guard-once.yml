name: Quarantine Target Overwrite Guard Once

on:
  push:
    branches: [dev]
    paths:
      - '.github/quarantine-target-overwrite-guard-once.js'
      - '.github/workflows/quarantine-target-overwrite-guard-once.yml'

permissions:
  contents: write

jobs:
  guard:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
        with:
          ref: dev
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Apply target overwrite guard
        run: node .github/quarantine-target-overwrite-guard-once.js
      - name: Remove one-off files before validation
        run: |
          rm -f .github/quarantine-target-overwrite-guard-once.js
          rm -f .github/workflows/quarantine-target-overwrite-guard-once.yml
      - name: Validate source
        run: |
          node --check src/core/security/protection/quarantine.js
          node --check src/core/administration/mod/quarantineInteractions.js
          npm run doctor
      - name: Commit guard
        run: |
          set -euo pipefail
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "fix(security): reject quarantine overwrite bypasses"
          git push origin HEAD:dev
