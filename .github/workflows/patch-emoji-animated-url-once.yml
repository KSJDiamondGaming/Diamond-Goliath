name: Patch Emoji Animated URL Once

on:
  push:
    branches: [dev]
    paths:
      - '.github/workflows/patch-emoji-animated-url-once.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout dev
        uses: actions/checkout@v5
        with:
          ref: dev
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply canonical animated emoji URLs
        run: node scripts/patch-emoji-animated-url-once.js

      - name: Validate changed source
        run: node --check src/modules/utilityStudio/emojis/emojis.js

      - name: Remove temporary patch files
        run: rm -f scripts/patch-emoji-animated-url-once.js .github/workflows/patch-emoji-animated-url-once.yml

      - name: Run project doctor
        run: npm run doctor

      - name: Commit clean feature
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m 'fix(emojis): expose canonical animated CDN URLs'
          git fetch origin dev
          git rebase origin/dev
          git push origin HEAD:dev
