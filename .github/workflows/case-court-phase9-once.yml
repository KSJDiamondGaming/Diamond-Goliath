name: Case Court Phase 9 Once

on:
  push:
    branches: [dev]
    paths:
      - '.github/workflows/case-court-phase9-once.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: dev
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: npm
      - name: Apply Court permission hardening
        run: python3 .github/case-court-phase9.py
      - name: Validate source
        run: node --check src/core/administration/mod/caseCourt.js
      - name: Install dependencies
        run: npm ci
      - name: Validate imports
        run: node -e "require('./src/core/administration/mod/caseCourt'); require('./src/core/administration/mod/permissions');"
      - name: Remove one-off files before Doctor
        run: rm -f .github/workflows/case-court-phase9-once.yml .github/case-court-phase9.py
      - name: Run Doctor
        run: npm run doctor
      - name: Commit Court permission hardening
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix(mod): harden court mutation permissions'
          git push origin HEAD:dev
