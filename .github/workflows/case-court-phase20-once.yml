name: Case Court Phase 20 Once

on:
  push:
    branches: [dev]
    paths:
      - '.github/case-court-phase20.py'
      - '.github/workflows/case-court-phase20-once.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: dev
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - name: Apply atomic Court operation claims
        run: python3 .github/case-court-phase20.py
      - name: Validate source
        run: |
          node --check src/core/administration/mod/storage.js
          node --check src/core/administration/mod/caseCourt.js
          node --check src/core/administration/mod/cases.js
      - name: Install dependencies
        run: npm ci
      - name: Validate imports
        run: npm run imports
      - name: Remove one-off files before Doctor
        run: |
          rm -f .github/case-court-phase20.py
          rm -f .github/workflows/case-court-phase20-once.yml
      - name: Run Doctor
        run: npm run doctor
      - name: Commit atomic Court operation claims
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix(mod): make court operation claims database atomic' || exit 0
          git pull --rebase origin dev
          git push origin HEAD:dev
