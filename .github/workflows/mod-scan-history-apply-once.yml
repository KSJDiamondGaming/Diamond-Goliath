name: Apply Scan History Source Patch

on:
  push:
    branches: [dev]
    paths:
      - '.github/workflows/mod-scan-history-apply-once.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: dev
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Apply source patch
        run: node scripts/patch-mod-scan-history-temp.js
      - name: Remove temporary script
        run: rm -f scripts/patch-mod-scan-history-temp.js
      - name: Validate source
        run: |
          node --check src/core/administration/mod/interactions.js
          npm run doctor
          npx vite build src/dashboard --outDir ../../dist --emptyOutDir --logLevel warn
      - name: Commit and push
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A src/core/administration/mod/interactions.js scripts/patch-mod-scan-history-temp.js
          git commit -m 'fix(mod): manage member scan history snapshots'
          git fetch origin dev
          git rebase origin/dev
          git push origin HEAD:dev
      - name: Remove helper workflow
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin dev
          git checkout -B dev origin/dev
          git rm -f .github/workflows/mod-scan-history-apply-once.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'chore(mod): remove scan history patch workflow'
          git push origin HEAD:dev
